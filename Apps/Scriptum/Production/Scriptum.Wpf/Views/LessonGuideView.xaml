<UserControl x:Class="Scriptum.Wpf.Views.LessonGuideView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mdxam="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Titel, StringFormat='Guide: {0}'}" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <mdxam:MarkdownViewer Markdown="{Binding GuideText}" FontSize="14"/>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Zur&#252;ck" Width="100" HorizontalAlignment="Left" 
                Margin="0,10,0,0" Click="BackButton_Click"/>
    </Grid>
</UserControl>
