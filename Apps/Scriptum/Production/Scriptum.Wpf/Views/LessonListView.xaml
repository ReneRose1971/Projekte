<UserControl x:Class="Scriptum.Wpf.Views.LessonListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{Binding ModuleTitel}" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Lektionen" FontSize="16" Foreground="Gray"/>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding Lessons}" 
                 MouseDoubleClick="LessonList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Description}" Foreground="Gray"/>
                            <TextBlock Text="{Binding TargetLength, StringFormat='Zeichen: {0}'}" 
                                       FontSize="11" Foreground="DarkGray" Margin="0,2,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Details" Width="70" Margin="5,0" Click="DetailsButton_Click" Tag="{Binding}"/>
                            <Button Content="Start" Width="70" Margin="5,0" Click="StartButton_Click" Tag="{Binding}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Content="Back" Width="100" HorizontalAlignment="Left" 
                Margin="0,10,0,0" Click="BackButton_Click"/>
    </Grid>
</UserControl>
