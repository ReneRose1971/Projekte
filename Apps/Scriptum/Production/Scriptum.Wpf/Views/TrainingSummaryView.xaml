<UserControl x:Class="Scriptum.Wpf.Views.TrainingSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Trainings&#252;bersicht" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding SummaryText}" FontSize="14" TextWrapping="Wrap" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <Button Content="Modul&#252;bersicht" Width="200" Height="40" Margin="0,5" Click="ModulesButton_Click"/>
            <Button Content="Startseite" Width="200" Height="40" Margin="0,5" Click="HomeButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <!-- Placeholder for future buttons -->
        </StackPanel>
    </Grid>
</UserControl>
