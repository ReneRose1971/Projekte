<UserControl x:Class="SolutionBundler.WPF.Controls.ProjectDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Projekt-Name -->
        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Projekt:"
                   FontWeight="Bold"
                   VerticalAlignment="Center"/>
        <TextBlock Grid.Row="0" Grid.Column="2"
                   Text="{Binding Name}"
                   VerticalAlignment="Center"
                   FontSize="14"/>

        <!-- Pfad -->
        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Pfad:"
                   FontWeight="Bold"
                   VerticalAlignment="Top"
                   Margin="0,5,0,0"/>
        <TextBlock Grid.Row="2" Grid.Column="2"
                   Text="{Binding Path}"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"
                   FontFamily="Consolas"
                   FontSize="11"
                   Foreground="Gray"
                   Padding="5"
                   Background="#F5F5F5"/>

        <!-- Status -->
        <TextBlock Grid.Row="4" Grid.Column="0"
                   Text="Status:"
                   FontWeight="Bold"
                   VerticalAlignment="Center"/>
        <StackPanel Grid.Row="4" Grid.Column="2"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <Ellipse Width="12" 
                     Height="12"
                     Margin="0,0,8,0"
                     VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#4CAF50"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding FileExists}" Value="False">
                                <Setter Property="Fill" Value="#F44336"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="Datei existiert"/>
                        <Setter Property="Foreground" Value="#4CAF50"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding FileExists}" Value="False">
                                <Setter Property="Text" Value="Datei nicht gefunden"/>
                                <Setter Property="Foreground" Value="#F44336"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- Gruppe (editierbare ComboBox) -->
        <TextBlock Grid.Row="6" Grid.Column="0"
                   Text="Gruppe:"
                   FontWeight="Bold"
                   VerticalAlignment="Center"/>
        <ComboBox Grid.Row="6" Grid.Column="2"
                  x:Name="GroupComboBox"
                  Text="{Binding Group, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsEditable="True"
                  VerticalAlignment="Center"
                  Padding="5"
                  FontSize="13"
                  ToolTip="Waehlen Sie eine vorhandene Gruppe oder geben Sie eine neue ein (leer fuer keine Gruppe)"/>
    </Grid>
</UserControl>
