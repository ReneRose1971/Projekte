<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

  <!-- Brushes (use SolidColorBrush so Blend shows editable brushes) -->
  <SolidColorBrush x:Key="PrimaryBrush" Color="#FF5B8AD1" />
  <SolidColorBrush x:Key="AccentBrush" Color="#FF3AAFA9" />
  <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF5F7FB" />
    <SolidColorBrush x:Key="WindowTitleBarBrush" Color="#FF5B8AD1" />
  <SolidColorBrush x:Key="TextBrush" Color="#FF222222" />
  <SolidColorBrush x:Key="HighlightBrush" Color="#FFF7E08A" />
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#FF285393" />
    <SolidColorBrush x:Key="ButtonPressedBrush" Color="#FF5482C6" />

  <!-- Spacing / Padding -->
  <Thickness x:Key="ControlMargin">6,4,6,4</Thickness>
  <Thickness x:Key="LabelPadding">4,2,4,2</Thickness>
  <Thickness x:Key="WindowPadding">12</Thickness>

  <!-- Font sizes -->
  <sys:Double x:Key="SmallFontSize">12</sys:Double>
  <sys:Double x:Key="NormalFontSize">14</sys:Double>

  <!-- Global text settings -->
  <Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="FontSize" Value="{StaticResource NormalFontSize}" />
  </Style>

  <!-- Label style (use for labels) -->
  <Style x:Key="LabelStyle" TargetType="TextBlock">
    <Setter Property="Padding" Value="{StaticResource LabelPadding}" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="FontSize" Value="{StaticResource SmallFontSize}" />
  </Style>

  <!-- Button style (global) -->
  <Style TargetType="Button">
    <Setter Property="Margin" Value="{StaticResource ControlMargin}" />
    <Setter Property="Padding" Value="8,4" />
    <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="4">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}" />
      </Trigger>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="{StaticResource ButtonPressedBrush}" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Opacity" Value="0.5" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- Window default style: sets background and padding; does not replace titlebar visuals by template -->
  <Style TargetType="Window">
    <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
    <Setter Property="Padding" Value="{StaticResource WindowPadding}" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
  </Style>

  <!-- TextBox: a bit more inner padding -->
  <Style TargetType="TextBox">
    <Setter Property="Margin" Value="{StaticResource ControlMargin}" />
    <Setter Property="Padding" Value="6,4" />
    <Setter Property="FontSize" Value="{StaticResource NormalFontSize}" />
  </Style>

  <!-- GroupBox: slightly more consistent appearance -->
  <Style TargetType="GroupBox">
    <Setter Property="Margin" Value="6" />
    <Setter Property="Padding" Value="6" />
  </Style>

</ResourceDictionary>