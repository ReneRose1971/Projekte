<UserControl x:Class="TypeTutor.WPF.ModuleGuideDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TypeTutor.WPF"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <StackPanel Margin="8">
        <TextBlock Text="Title:" FontWeight="Bold"/>
        <TextBox Text="{Binding Selected.Title}" IsReadOnly="True"/>
        <TextBlock Text="Body (Markdown):" FontWeight="Bold" Margin="0,8,0,0"/>
        <wv2:WebView2 x:Name="WebView" Height="400" />
        <TextBox x:Name="FallbackBox" Visibility="Collapsed" Height="400" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </StackPanel>
</UserControl>