<UserControl x:Class="TypeTutor.WPF.ModuleGuideListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="List" Grid.Row="0" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" DisplayMemberPath="Title" SelectionChanged="OnSelectionChanged"/>

        <Button Grid.Row="1" Content="Importieren" Command="{Binding CmdImport}" HorizontalAlignment="Left" Margin="4" Width="120"/>
    </Grid>
</UserControl>