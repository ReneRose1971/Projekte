<UserControl x:Class="TypeTutor.WPF.TypingTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="140">

    <UserControl.Resources>
        <!-- initial transparent brush; animation will change color alpha -->
        <SolidColorBrush x:Key="HighlightBrush" Color="#00F7E08A" />
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Zusammenfassung -->
        <TextBlock Text="{Binding Summary}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6"
                   Grid.Row="0"/>

        <!-- Zieltext -->
        <GroupBox Header="Zieltext (Soll)" Grid.Row="1" Margin="0,0,0,6">
            <Border x:Name="TargetBorder">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource HighlightBrush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding JustLoaded}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <!-- animate from opaque highlight to transparent over 700ms -->
                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" From="#FFF7E08A" To="#00F7E08A" Duration="0:0:0.7" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding TargetText}"
                               TextWrapping="Wrap"
                               FontFamily="Consolas"
                               FontSize="14"
                               Padding="6"/>
                </ScrollViewer>
            </Border>
        </GroupBox>

        <!-- Eingabetext -->
        <GroupBox Header="Eingabe (Ist)" Grid.Row="2">
            <Border x:Name="InputBorder" Background="Transparent">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding InputText}"
                               TextWrapping="Wrap"
                               FontFamily="Consolas"
                               FontSize="14"
                               Padding="6"/>
                </ScrollViewer>
            </Border>
        </GroupBox>

    </Grid>
</UserControl>
